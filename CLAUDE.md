# CLAUDE.md — AI Assistant Guide for Effulgence

This file provides context and conventions for AI assistants (e.g., Claude) working in this repository.

## Project Overview

**Effulgence** is a project under the **devitools** organization. This repository is in its initial stage and will be updated as the project takes shape.

## Repository Status

This project is newly initialized. There is no source code, build configuration, or CI/CD pipeline yet. As the project develops, this document should be updated to reflect the current state.

## Directory Structure

```
effulgence/
├── CLAUDE.md          # This file — AI assistant guide
└── (project files to be added)
```

> **Update this section** as directories and files are added.

## Development Workflow

### Git Conventions

- **Branch naming**: Use descriptive branch names prefixed with a category (e.g., `feat/`, `fix/`, `docs/`, `refactor/`).
- **Commit messages**: Write clear, imperative-mood messages (e.g., "Add user authentication module", not "Added user authentication module").
- **Pull requests**: Include a summary of changes, motivation, and any testing performed.

### Build & Run

> **TODO**: Document build commands, scripts, and run instructions once the project is set up.

### Testing

> **TODO**: Document testing framework, how to run tests, and coverage expectations once tests are added.

### Linting & Formatting

> **TODO**: Document linter and formatter configuration once tooling is in place.

## Key Conventions

### Code Style

- Prefer clarity over cleverness.
- Keep functions small and focused on a single responsibility.
- Name variables and functions descriptively.

### Architecture

> **TODO**: Document architecture decisions and patterns as they are established.

### Dependencies

> **TODO**: Document key dependencies and their purposes once added.

## For AI Assistants

When working in this repository:

1. **Read before writing** — Always read existing files before modifying them.
2. **Stay focused** — Only make changes that are directly requested. Avoid unnecessary refactoring or additions.
3. **Keep this file updated** — When you make significant structural changes (new directories, new tooling, new conventions), update this CLAUDE.md accordingly.
4. **Follow existing patterns** — Match the style and conventions already present in the codebase.
5. **Test your changes** — Run the project's test suite (once established) before considering work complete.
6. **Don't over-engineer** — Prefer simple, direct solutions. Avoid premature abstractions.
